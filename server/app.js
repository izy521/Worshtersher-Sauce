"use strict";
/* I prefer using strict mode
 * I also prefer using ES5, but I'll use some ES6 
 * to prove I'm not stuck in the stone age =o
 */
var PORT = 3000;
var GAME = require('./Game/GAME.js');

var game = new GAME(PORT);
